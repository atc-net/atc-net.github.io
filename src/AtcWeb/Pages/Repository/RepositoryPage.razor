@page "/repository/{RepositoryName}"

@if (isLoaded && repository is null)
{
    <MudText Typo="Typo.h5">Repository '@RepositoryName' was not found.</MudText>
}
else
{
    <DocsPageRepository @key="RepositoryName" Repository="@repository" />
}