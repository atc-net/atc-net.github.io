<MudGrid Justify="Justify.Center">
    @if (Contributors is null)
    {
        <MudProgressCircular Color="Color.Primary" Size="Size.Medium" Indeterminate="true" />
    }
    else
    {
        @foreach (var contributor in Contributors.OrderBy(x => x.Name))
        {
            <MudItem xs="12" md="6">
                <GitHubContributorItem Name="@contributor.Name"
                                       Title="@ItemTitle"
                                       AvatarLink="@contributor.AvatarUrl"
                                       GithubLink="@contributor.Url"/>
            </MudItem>
        }
    }
</MudGrid>

@code {
    [Parameter]
    public List<Domain.GitHub.Models.GitHubContributor>? Contributors { get; set; }

    [Parameter]
    public string ItemTitle { get; set; } = "Contributor";
}