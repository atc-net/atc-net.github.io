@inherits MarkdownRepositoryContentBase
@inject IJSRuntime JS

<div class="markdown">
    @HtmlContent
</div>

@code {
    protected override async Task OnAfterRenderAsync(bool firstRender)
    {
        await JS.InvokeVoidAsync("renderMermaidDiagrams");
    }
}